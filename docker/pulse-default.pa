#!/usr/bin/pulseaudio -nF

# Minimal PulseAudio config for MeetBeats
# The null sink is created by entrypoint.sh after PulseAudio starts

.fail

# Load native protocol for local communication (allow anonymous access in Docker)
load-module module-native-protocol-unix auth-anonymous=1
